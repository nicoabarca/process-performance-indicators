import pandas as pd


def events(case: pd.DataFrame) -> set:
    """
    Get the events set of a case.

    Args:
        case: The case to get the events set of.

    Returns:
        The events set of the case.

    """
    # TODO: [fns_case] Implement this function
